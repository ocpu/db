package io.opencubes.db.sql.select

/**
 * A enum that indicates how results should be ordered
 */
enum class Order {
  /**
   * Order results in ascending order.
   */
  ASCENDING,

  /**
   * Order results in descending order.
   */
  DESCENDING
}
